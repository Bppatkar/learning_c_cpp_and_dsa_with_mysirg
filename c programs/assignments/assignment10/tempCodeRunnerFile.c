// 4. Write a program to calculate HCF of two numbers
#include <stdio.h>
int main()
{
  int i, a, b, H;
  for (i = 0; H <= a; i++)
  {
    if (a % 2 == 0 && b % 2 == 0)
    {
      printf("HCF is %d", H);
    }
  }
  return 0;
}